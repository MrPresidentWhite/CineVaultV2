# Docker-Stack: Von docker-compose genutzt. Kopie als .env anlegen und anpassen.
# DATABASE_URL und REDIS_URL werden von docker-compose aus den folgenden Werten gesetzt.
# Werte mit $ (z. B. PASSWORD_PEPPER): $ als $$ schreiben, sonst liest Compose $xyz als Variable.

# --- Postgres (persistentes Volume postgres_data) ---
POSTGRES_USER=cinevault
POSTGRES_PASSWORD=bitte-setzen
POSTGRES_DB=cinevault
POSTGRES_PORT=5432

# --- Redis (persistentes Volume redis_data) ---
REDIS_PASS=bitte-setzen
REDIS_PORT=6379

# --- App (öffentliche URL; Port nur intern) ---
APP_URL=http://localhost:3000

# --- Nginx (einziger öffentlicher Eintritt) ---
NGINX_HTTP_PORT=80

# --- Pflicht für die App (Rest wie in .env.example) ---
ENVIRONMENT=prod
SESSION_SECRET=bitte-langes-geheimes-token-setzen
TMDB_API_KEY=

# Optional: R2, SMTP, Admin, etc. – siehe .env.example
